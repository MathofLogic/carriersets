# Substance bug

An output of a running process is frozen into a container.
Relations are declared intrinsic to that container.
Establishment and maintenance are priced at 0.
`A=A` is written free so the container is "the same."

Subject-predicate grammar is that rewrite in speech.
"S is P" is not more real than the isolations it shortened.

```
PYTHONPATH=. python3 -m studio.substance
```

| grammar | process | skip |
|---|---|---|
| x has derivative 6 | rate(x*x, x) | 6 put inside x |
| (9,6) is one object | two mints | value-eq merge |
| 5 is 5 | wrap tick | numeral owns identity |
| True is a bit-thing | Q refuses bool | wrap treated as Q |
| electron has spin | reading under G | click owns spin |
| DNA has a code | complement program | polymer owns meaning |
| S is P | isolate; read channel | noun outranks process |

Not claimed: this file recovers physics or linguistics.
It maps one rewrite. Paid rows for the merge and the wrap
already live in generated atlas and `studio.eq`.
