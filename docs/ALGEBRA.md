# Algebra breakdown

Simpler than calc: one slot until you isolate.

## Knobs

| | |
|---|---|
| V | listed alphabet you will add and multiply |
| G | + and ×, and the inverses that exist |
| θ | no additive inverse, no unit, leave V, unlistable V |

A letter is a **hole** in an expression. Evaluating fills holes from V.
It is not a box that contains a number.

## What closes

- `Z/p` (p prime): field. Every nonzero has a ×-inverse.
- `Z/4`, `Z/6`: + is a group, × is not a field. 2 has no inverse in Z/4.
- `{0,1}` with ordinary + : `1+1=2` leaves V. That is not Z/2.
- Q, Z, R: unlistable. Do not census. Use them as names of *other* cuts.

## Solving

`2x+3=0` in Z/5 is search: which listed x makes the expression 0.
No “unknown quantity.” Roots of `x²+1` in Z/5: 2 and 3 (because 4+1≡0).
Hiring `i` is a leftover of the *same letters* on R, not a law of the
polynomial. Different V, different roots.

## Ghosts

- Number-as-thing that operations discover
- Variable-as-box
- Equation as two names for one essence
- `i` as a point on the old line
- “the integers” as a finished listed V

## Packing

Recipes → holes (symbols) → chosen V → “field” = every nonzero inverts.
Complex is a bigger V, not a spirit in the leftover of `x²+1` on R.
