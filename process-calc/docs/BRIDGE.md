# Bridge sheet — pass the other exam without putting the ghost back

Write their symbols. Keep our bookkeeping in the margin.

| they write | you did | say if asked |
|---|---|---|
| \(f'(x)\) | `rate(f, x)` | “derivative with respect to x” |
| \(\frac{dy}{dx}\) | `rate(y, x)` | two channels; dx cancelled if a chain |
| \(\frac{d}{dx}(uv)=u'v+uv'\) | Leibniz mix | product rule |
| chain | `rate(q,y)·rate(y,x)` | chain rule |
| \(\lim_{h\to 0}\frac{f(x+h)-f(x)}{h}\) | we did not take that limit | “I used the algebraic rate; same number on polynomials” |
| vertical asymptote | θ | “undefined / does not exist” |
| related rates | second reading | same steps, their layout |
| \(f''(x)\) | jet r2 or say first-course only | if the exam needs it, use their second-deriv rules |
| \(\int_a^b f\) | listed sum or antiderivative | FTC: rate of F is f |
| “let h→0” | keep leftover | do not write that sentence unless they demand it |

Do not argue ontology in the exam booklet. The certificate is for *this* course.
