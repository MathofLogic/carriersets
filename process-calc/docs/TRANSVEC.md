# Trig, exp, log, vector

Not in Q as values at a generic mark. In this kernel as:

- initial readings that *are* in Q
- rate relations (G)
- finite jets; leftover named
- several isolators for vector

```
PYTHONPATH=process-calc:. python3 -m pcalc.translab
PYTHONPATH=process-calc:. python3 -m pcalc.veclab
```

| G | at | reading | rate |
|---|---|---|---|
| exp-jet | 0 | (1,1) | 1 = value |
| sin-jet | 0 | (0,1) | 1 = cos.v |
| cos-jet | 0 | (1,0) | 0 = -sin.v |
| log1p-jet | 0 | (0,1) | 1 |
| log | 0 | θ | — |

exp-jet(1, n=5) = 163/60. That is not e. Grow V or name the leftover.

grad(x²y) at (2,3) = (12, 4).
div(x,y) = 2.
curl(-y, x) = 2.
curl(grad f) = 0.

No R^n furniture. No circle-as-substance.
